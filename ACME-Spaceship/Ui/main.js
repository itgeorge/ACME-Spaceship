function init() {
    var canvasEl = document.getElementById("viewport.js");
    var canvasContext = canvas.getContext("2d");

    //Initialize renderer by canvas context
    //Initialize engine by renderer and attach ui events
}

function onKeyDown(event) {
    
}